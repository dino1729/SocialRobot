faster-whisper==1.0.3
onnxruntime-gpu==1.23.2
openai-whisper==20250625
piper-tts==1.3.0
scipy==1.15.3
torch==2.9.0
torchaudio==2.9.0

# Chatterbox TTS (Zero-shot voice cloning)
# IMPORTANT: For RTX 50-series GPUs (RTX 5090, 5080, etc.), you MUST install PyTorch 2.9.1+ with CUDA 12.8 FIRST
# These GPUs use Blackwell architecture (sm_120) which is not supported by older PyTorch versions
#
# Installation order for RTX 50-series:
#   1. pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   2. pip install chatterbox-tts torchcodec
#   3. pip install perth s3tokenizer conformer==0.3.2 diffusers==0.29.0 pykakasi==2.3.0 resemble-perth==1.0.1
#   4. pip install pkuseg==0.0.25 --no-build-isolation
#
# For RTX 40-series and older GPUs, the packages below should work directly after torch/torchaudio
chatterbox-tts>=0.0.1
torchcodec>=0.8.0
perth>=1.0.0
s3tokenizer>=0.2.0
conformer==0.3.2
diffusers==0.29.0
pykakasi==2.3.0
resemble-perth==1.0.1
# pkuseg==0.0.25  # Install separately with: pip install pkuseg==0.0.25 --no-build-isolation

# VibeVoice TTS (Microsoft's realtime streaming TTS with ~300ms latency)
# Installation:
#   git clone https://github.com/microsoft/VibeVoice.git
#   cd VibeVoice && pip install -e .
#
# Required dependencies (automatically installed with VibeVoice):
accelerate==1.6.0
transformers==4.51.3
flash-attn  # For GPU acceleration (requires CUDA)
# Note: vibevoice package itself must be installed from git (pip install -e .)
# The model will be downloaded from HuggingFace: microsoft/VibeVoice-Realtime-0.5B
